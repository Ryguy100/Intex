<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/scss/custom.css" />
    <link rel="stylesheet" href="/css/styles.css" />
    <title>Home</title>
    <style>
      a:hover {
        color: rgb(129, 108, 108);
      }
    </style>
  </head>

  <!--Additional styling added to this page for its unique needs-->

  <style>
   
    .popup-container {
      position: fixed;
      bottom: 10px;
      right: 10px;
      /* box-shadow: 0 0 10px rgba(0, 0, 0, 0.2); */
      padding: 10px;
      /* border-radius: 5px; */
      display: block;
      max-width: 270px;
      text-align: center;
      transition: all 0.2s ease-in-out;
    }
      .popup-content {
        font-size: 14px;
        color: #333;
      }

      .popup-button {
      position: absolute;
      top: -20px;
      right: 10px;
      width: 20px;
      height: 20px;
      background-color: #ae7fcf;
      color: #fff;
      border: none;
      border-radius: 2px;
      cursor: pointer;
      text-align: center;
      line-height: 30px;
      transition: all 0.2s ease-in-out;
    }
  </style>
  <body>

    <!-- Navigation Bar -->

    <nav class="navbar navbar-expand-lg">
      <div style="padding-left: 1rem">
        <button
          class="navbar-toggler"
          type="button"
          data-toggle="collapse"
          data-target="#navbarNav"
          aria-controls="navbarNav"
          aria-expanded="false"
          aria-label="Toggle navigation"
        >
          <span class="navbar-toggler-icon"></span>
        </button>
      </div>

      <div class="navbar-brand mx-auto">
        <img src="/logo.png" alt="Brand Logo" style="max-height: 30px" />
      </div>

      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav" style="padding-left: 2rem">
          <li class="nav-item">
            <a class="nav-link" href="/">Home</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/dashboard">Dashboard</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/survey">Survey</a>
          </li>

          <% if (user) { if (user.isadmin) { %>
          <li class="nav-item">
            <a class="nav-link" href="/data">See Data</a>
          </li>
          <% } } %>
        </ul>
      </div>
      <% if(user) { %>

      <div class="account-dropdown">
        <button
          class="btn btn-link dropdown-toggle"
          type="button"
          id="accountDropdown"
          data-toggle="dropdown"
          aria-haspopup="true"
          aria-expanded="false"
        >
          My Account
        </button>
        <div class="dropdown-menu" aria-labelledby="accountDropdown">
          <a class="dropdown-item"
            ><form method="get" action="/edit/<%= user.id %>/">
              <button
                style="border: none; background-color: white"
                type="submit"
              >
                Edit Account
              </button>
            </form></a
          >
          <% if (user.isadmin) { %>
          <a class="dropdown-item" href="/users">Edit Users</a>
          <% } %>
          <div class="dropdown-divider"></div>
          <a class="dropdown-item" href="/logout">Logout</a>
        </div>
      </div>

      <% } else { %>

      <div class="account-dropdown">
        <button
          class="btn btn-link dropdown-toggle"
          type="button"
          id="accountDropdown"
          data-toggle="dropdown"
          aria-haspopup="true"
          aria-expanded="false"
        >
          <a href="/login">Login</a>
        </button>
        <div class="dropdown-menu" aria-labelledby="accountDropdown">
          <a class="dropdown-item" href="/login">Login</a>
          <a class="dropdown-item" href="/register">Register</a>
        </div>
      </div>

      <% } %>
    </nav>

    <!-- Navigation Bar Ending-->

    <!-- Header to bring attention to the main purpose of the webstite -->

    <div class="sleek-header">
      <h1>Provo City Mental Health Awareness</h1>
      <p>
        We are gathering responses to find a link between social media usage and
        mental health
      </p>
      <a href="/survey" class="btn btn-primary sleek-button">Take the survey</a>
    </div>

    <!-- Body -->

    <!-- BIG IDEA -->

    <div class="sample-text">
      <p style="color: rgb(91, 88, 88);">The results of our study shows that</p>
      <h4>Increased social media use leads to higher rates of depression</h4>
    </div>

    <!-- Entices the user to explore the site -->

    <div class="container mt-5">
      <div style="text-align: center;">
        <h4>Explore our site</h4><br>
      </div>
      
      <div class="row flex-nowrap">
        <div class="col-md-6 mb-4">
          <div class="card">
            <a href="/dashboard" style="color: black; text-decoration: none;">
            <img src="pie.jpeg" class="card-img-top" alt="Photo 2" />
            <div class="card-body">
                <h5 class="card-title">See Graphs</h5>
                <p class="card-text">
                  Discover the link between social media usage and mental health
                </p>
              </a>
            </div>
          </div>
        </div>

        <!-- Card with groovy background -->
        <div class="col-md-6 mb-4">
          <div class="card groovy-background text-white">
            <div class="card-body">
              <h5 class="card-title">Mental Health Resources</h5>
              <p class="card-text">Visit the official website from the CDC about mental health</p>
              <button type="button" onclick="window.location.href='https://www.cdc.gov/mentalhealth/index.htm'" style="color: white; background-color: transparent; border: none; color: white; box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);" class="btn btn-light">See More</button>
            </div>
          </div>
        </div>
      </div>
    </div>

    <div class="container">
      <div class="sample-text bg-light">
        <h4>Why we built this website</h4>
        <p style="font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;">
          This website addresses the vital link between mental health and social media. In our digital age, social media's impact on well-being is a pressing concern. The site offers valuable insights, resources, and support to help individuals navigate the digital landscape wisely. By raising awareness, Provo aims to empower the community for healthier online interactions. Additionally, we invite you to take our survey to contribute valuable data and insights to this important conversation.
        </p>
      </div>
    </div>

    <div class="sample-text">
      <h4>Motivational Quote</h4>
      <p><%= chat%></p>
    </div>
    
<script>
    // Variable to track the state of the popup (minimized or not)
    var isMinimized = false;
  // Function to toggle between minimizing and maximizing the popup
  function togglePopup() {
    var popup = document.getElementById("minimizablePopup");
    var button = document.querySelector(".popup-button");
   
    // Check if the popup and button exist
    if (popup && button) {
      if (isMinimized) {
        // If the popup is minimized, restore it to the normal state
        popup.style.height = "auto"; // Reset height
        button.style.top = "-30px"; // Move button to the top
        isMinimized = false;
      } else {
        // If the popup is not minimized, set it to the desired minimized height
        popup.style.height = "30px"; // Set the desired minimized height
        button.style.top = "10px"; // Move button back to its original position
        isMinimized = true;
      }
    }
  }
</script>

    
<!-- Footer -->
    <footer class="footer">
      <div class="container">
        <p>&copy; 2023 City of Provo. All Rights Reserved.</p>
      </div>
    </footer>

    <!--Scripts for the Bootstrap-->
    <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"></script>
  </body>
</html>
